```{=html}
<ul class="pub-list list">
<% for (const item of items) { %>
  <li <%= metadataAttrs(item) %>>
    <span class="listing-title lead"><%= item.title %></span><br />
    <span class="listing-author"><%= item.author %></span><br />
    <span class="listing-publication fst-italic"><%= item.publication %></span>
    <span class="listing-year">(<%= item.year %>)</span><br />

    <a href="<%- item.path %>" class="btn btn-primary">Details</a>

    <% if (item.doi) { %>
      <a href="<%- item.doi %>" class="btn btn-secondary" target="_blank" rel="noopener noreferrer">
        DOI
      </a>
    <% } %>

    <% if (item.preprint) { %>
      <a href="<%- item.preprint %>" class="btn btn-blue" target="_blank" rel="noopener noreferrer">
        Paper
      </a>
    <% } %>

    <% if (item.materials) { %>
      <a href="<%- item.materials %>" class="btn btn-green" target="_blank" rel="noopener noreferrer">
        Code
      </a>
    <% } %>

    <% if (item.poster) { %>
      <a href="<%- item.poster %>" class="btn btn-purple" target="_blank" rel="noopener noreferrer">
        Poster
      </a>
    <% } %>

  </li>
<% } %>
</ul>

```
